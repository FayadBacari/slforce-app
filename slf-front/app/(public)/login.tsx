import { LoginScreen } from '@modules/authentication/presentation/screens/login-screen';

// Expo Router wraps the screen component.
// The actual screen logic and UI is in the module — not here.
export default function LoginPage() {
  return <LoginScreen />;
}
